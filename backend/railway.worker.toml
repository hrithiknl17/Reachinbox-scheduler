[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && npx prisma generate"

[deploy]
startCommand = "node dist/workers/emailWorker.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
